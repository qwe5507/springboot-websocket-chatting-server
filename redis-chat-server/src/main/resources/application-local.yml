spring:
  redis:
    host: localhost
    port: 6379

management:
  endpoint:
    health:
      #      show-details: always
      show-components: always
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
